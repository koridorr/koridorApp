import React from 'react';
import OrderDetailsView from './OrderDetailsView';

const OrderDetails = (props: any) => {
  // const {deliver} = {};

  return <OrderDetailsView deliver={false}  // deliver={deliver} 
  />;
};

export default OrderDetails;
